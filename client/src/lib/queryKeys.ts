/**
 * Centralized React Query keys to keep cache access consistent and typo-free.
 */
export const queryKeys = {
  home: ['home'] as const,
  features: ['features'] as const,
  testimonials: ['testimonials'] as const,
}
